const isPlateDigitAffected = require('./isPlateDigitAffected')

test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',1)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',2)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',3)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',4)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',5)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',6)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',7)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',8)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',9)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Monday',0)).toBe(false)
})


test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',1)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',2)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',3)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',4)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',5)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',6)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',7)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',8)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',9)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Tuesday',0)).toBe(false)
})

test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',1)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',2)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',3)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',4)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',5)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',6)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',7)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',8)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',9)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Wednesday',0)).toBe(false)
})

test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',1)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',2)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',3)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',4)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',5)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',6)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',7)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',8)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',9)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Thursday',10)).toBe(false)
})

test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',1)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',2)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',3)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',4)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',5)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',6)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',7)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',8)).toBe(false)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',9)).toBe(true)
})
test('properly verifies if license plate digit can drive during the day consulted', () => {
	expect(isPlateDigitAffected('Friday',0)).toBe(true)
})



